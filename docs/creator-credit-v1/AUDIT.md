# Creator credit v1 — famtastic-hosting

Source branch: codex/creator-credit-20260918, based on freshly fetched origin/main. Original checkout left untouched. Scope: this repository only.

Approved logo: famtastic-designs-logo-v1.png, 2172 × 724 RGBA PNG, SHA-256 ebb0477344132d32e449ba19e2b622921585aa71af0decdbcf8abfbe033fa950. Owner-provided source verified locally; byte copies only, no redraw/recolor. The neutral backing is CSS around the original PNG.

Owner mandate (2026-09-18): preserve existing footer/copyright/credit and add the exact approved FAMtastic Designs PNG as a final centered linked row. Exceptions require an explicit owner override, never a tier inference. Use https://famtasticdesigns.com/ with optional public slug UTM attribution only; no new tracking scripts/cookies. Preserve the logo bytes, accessible link name, responsive 160–220px image and >=44px target. Verify fixed controls cannot obscure it. Historical approvals and evidence remain immutable; derive a new version when changing an approved proof.

Implementation preserves previous footer HTML and adds one final row. Only logo, presentation files and explicit packaging changes belong in the deployment. No auth logic, data, financial state, customer messages or runtime configuration is changed.

Verification at this checkpoint: npm test passed. Build/validator status and browser evidence are recorded separately. Early standalone browser diagnostics were stopped after the owner required CUA-only interactions. They are preliminary diagnostics, not final release approval. Current CUA/live records supersede them.

Release status at this source checkpoint: NOT DEPLOYED. Owner has authorized existing-live-surface retrofit after MBSH96, but each target still requires exact baseline, source commit, scoped publish list and independent live evidence. Do not infer ecosystem completion from this repository.

Design addendum: compact centered 160–220px image; >=44px accessible link; neutral obsidian link backing; no scripts/cookies; public site slug only. Existing design.md and its historical hash remain intact. This is the separately versioned addition.
